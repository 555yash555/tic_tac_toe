import 'package:flutter/material.dart';
const neon_color=Color.fromARGB(238, 3, 207, 6);
const score_font=TextStyle(fontSize: 25,);
AppBar app_bar=AppBar(
        title: Text(
          "TIC-TAC-TOE",
          style: TextStyle(color: neon_color),
        ),
      );
const playerinfo=TextStyle(fontSize:35,color:neon_color);